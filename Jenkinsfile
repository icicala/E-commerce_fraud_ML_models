pipeline {
    agent any
    stages {
        stage('Feature Creation') {
            steps {
                sh 'python --version && python main.py'
            }
        }
        // Add more stages
    }
}