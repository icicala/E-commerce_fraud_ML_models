pipeline {
    agent any
    stages {
        stage('Feature Creation') {
            steps {
                sh 'python3 --version && python3 main.py'
            }
        }
        // Add more stages
    }
}